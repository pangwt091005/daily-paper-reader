---
title: Food Image Segmentation with LLM-Derived Ingredient Labels and Multimodal Fusion
title_zh: 基于大语言模型推导的食材标签与多模态融合的食物图像分割
authors: "Jui-Feng Chi, Wei-Ta Chu, Sheng-Long Lin"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25820v1"
tags: ["query:nutritionk"]
score: 9.0
evidence: 利用LLM推导的食材标签和多模态融合进行食品图像分割，实现基于图像的营养成分估计。
tldr: 食物图像分割在营养追踪等健康应用中至关重要，但现有模型对视觉相似食材和罕见类别表现欠佳。本文提出两个即插即用多模态模块LIM-F和LIM-Q，利用大语言模型从图像推断食材标签并注入视觉分析流程，无需预先对齐图文。在FoodSeg103基准上，集成LIM-Q的Mask2Former（Swin-L）达到55.0 mIoU，LIM-F也达54.4，且GPU内存增加不超过3.8 GB，为细粒度食物理解提供了实用可扩展方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25820v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1233, \"height\": 455, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25820v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1232, \"height\": 554, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25820v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1225, \"height\": 644, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25820v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 981, \"height\": 646, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25820v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1234, \"height\": 674, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.25820v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 789, \"height\": 433, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25820v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1335, \"height\": 276, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25820v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 969, \"height\": 396, \"label\": \"Table\"}]"
motivation: 现有食物图像分割模型在视觉相似食材和稀有类别上性能不足，需要引入额外语义信息。
method: 提出LIM-F（注入特征）和LIM-Q（注入查询）两个模块，利用LLM从图像推断食材标签，直接注入视觉编码器或Transformer解码器。
result: 在FoodSeg103上，Mask2Former+Swin-L+ LIM-Q达55.0 mIoU，LIM-F达54.4，CNN架构从47.7提升至49.8 mIoU。
conclusion: 该方法以适度GPU内存增加（≤3.8 GB）实现SOTA性能，为细粒度食品理解提供实用可扩展方案。
---

## 摘要
食物图像分割在营养追踪和个性化健康监测等健康相关应用中起着关键作用。然而，现有模型在视觉相似的食材和稀有食物类别上往往表现不佳。为解决这一问题，我们提出了两个即插即用的多模态模块，通过利用大语言模型从食物图像中推断出的食材标签来增强分割性能。第一个模块称为LIM-F（面向特征的语言注入模块），设计用于与任何产生多层输出的图像编码器（如Swin Transformer）配对；第二个模块LIM-Q（面向查询的语言注入模块）则针对Mask2Former风格的基于Transformer的解码器。这两个模块通过直接将语义食材信息注入视觉分析流程，无需预先对齐图像与文本即可训练。在FoodSeg103基准上，所提方法达到了最先进的性能。具体而言，将LIM-Q集成到基于Swin-L图像编码器的Mask2Former解码器中，平均交并比达到55.0。在同一模型（Swin-L+Mask2Former）下，LIM-F也展现出强大的泛化能力和有竞争力的性能，mIoU达到54.4。此外，其适用性超越了基于Transformer的解码器，当集成到基于CNN的架构中时，mIoU从47.7提升至49.8。值得注意的是，改进的分割精度仅需在训练期间适度增加（最多3.8 GB）的GPU内存消耗。因此，所提方法为细粒度食物理解提供了一种实用且可扩展的解决方案。

## Abstract
Food image segmentation plays a vital role in health-related applications such as nutrition tracking and personalized health monitoring. However, existing models often underperform on visually similar ingredients and rare food categories. To address this issue, we propose two plug-and-play multimodal modules that enhance the segmentation performance by leveraging ingredient labels inferred from food images using large language models (LLMs). The first module, called LIM-F (Language Injection Module for Features), is designed to pair with any image encoder that produces multi-layer outputs (e.g., Swin Transformer), while the second module, LIM-Q (Language Injection Module for Queries), targets Mask2Former-style Transformer-based decoders. Both modules enable training without the need for pre-aligning images with text by directly injecting semantic ingredient information into the visual analysis pipeline. On the FoodSeg103 benchmark, the proposed method achieves state-of-the-art performance. Specifically, integrating LIM-Q into the Mask2Former decoder with a Swin-L image encoder yields a mean Intersection over Union (mIoU) of 55.0. LIM-F also demonstrates strong generalization and competitive performance, reaching an mIoU of 54.4 under the same model (Swin-L+Mask2Former). Furthermore, its applicability extends beyond Transformer-based decoders, as evidenced by an improvement from 47.7 to 49.8 mIoU when integrated into a CNN-based architecture. Notably, the improved segmentation accuracy is achieved with only a moderate (at most 3.8 GB) increase in the GPU memory consumption during training. Thus, the proposed approach offers a practical and scalable solution for fine-grained food understanding.

---

## 论文详细总结（自动生成）

# 基于大语言模型推导的食材标签与多模态融合的食物图像分割——论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：食物图像分割在营养追踪、饮食评估、个性化健康监测等应用中至关重要。但现有模型在面对视觉高度相似的食材（如豆腐和奶酪）以及稀有食物类别时，分割性能往往不足。传统方法依赖大规模图文对齐数据集（如Recipe1M+）或复杂的预训练流程，限制了可扩展性和适应性。
- **核心问题**：如何在不依赖大规模对齐图文对的前提下，将食材语义信息有效注入视觉分割管道，以提升对相似和稀有食材的分割精度。
- **整体含义**：本文提出两个即插即用的多模态融合模块（LIM-F和LIM-Q），利用大语言模型从图像中推断食材标签，并直接注入特征层或查询层，实现了无需预对齐、轻量级的食材语义引导，显著提升了FoodSeg103基准上的分割性能。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：利用大语言模型（GPT-o4 mini）从输入图像和食材类别列表推断出图像中可能存在的食材标签，然后通过BERT模型编码为文本嵌入，再通过两个定制模块（LIM-F或LIM-Q）将语义信息注入分割网络。
- **关键技术细节**：
  - **整体架构**：双分支结构。视觉分支采用Swin-L（可替换为其他多级输出骨干）提取多尺度特征；语言分支由LLM生成食材标签列表，经BERT编码得到词嵌入。
  - **LIM-F（特征级注入模块）**：设计用于与多级输出骨干（如Swin-L）配合。在每个特征层级，独立执行交叉注意力：以视觉特征为查询（Query），BERT编码的食材嵌入为键（Key）和值（Value），输出通过残差连接加回原特征，再经过前馈网络。适用于任何不限制解码器类型的分割架构（如K-Net+UPerNet或Mask2Former）。
  - **LIM-Q（查询级注入模块）**：针对Mask2Former风格的Transformer解码器设计。在每个解码轮次开始前，将可学习查询向量与BERT编码的食材嵌入进行交叉注意力，再经自注意力和前馈网络，残差连接。循环注入确保语义信息持续引导解码过程。
  - **损失函数**：
    - K-Net+UPerNet变体使用标准交叉熵损失。
    - Mask2Former变体使用掩码交叉熵损失（L_CE）、Dice损失（L_dice）和分类损失（L_cls），加权求和：L = λ_CE L_CE + λ_dice L_dice + λ_cls L_cls。实验中通过调整λ_cls获得最佳性能。
  - **即插即用特性**：无需修改骨干网络或标注格式，可直接集成到现有分割架构；无需图像-文本对齐预训练。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：FoodSeg103基准数据集（包含103类食材，来源于Recipe1M+的图像）。
- **基准与对比方法**：
  - 对比了BEiTv2 Large（49.4 mIoU）、IngredSAM（48.8）、FoodSAM（46.4）、Swin-TUNA（50.6）、K-Net（Swin-L）（47.7）、Mask2Former（Swin-L）（51.9）。
  - 本文提出的三个变体：
    1. K-Net（Swin-L）+ LIM-F → 49.0 mIoU（比基线+2.7%）
    2. Mask2Former（Swin-L）+ LIM-F → 54.4 mIoU（+4.8%）
    3. Mask2Former（Swin-L）+ LIM-Q → **55.0 mIoU（+6.0%），达到新SOTA**。
  - 另外，LIM-F集成到CNN架构（K-Net）也验证了其通用性。

## 4. 资源与算力

- **GPU型号与数量**：论文未明确说明具体GPU型号（如A100或V100）及数量。
- **训练时长与内存**：
  - 批大小：2，训练迭代：240,000次。
  - GPU内存：原始Mask2Former约19 GB；Mask2Former+LIM-F为22.5 GB；Mask2Former+LIM-Q为20 GB。即最多增加3.8 GB。
- **优化器与学习率**：AdamW优化器；Swin-L骨干和BERT最后四层使用学习率乘数0.1；基础学习率1e-4（Mask2Former变体）或6e-5（K-Net变体），余弦退火衰减至1e-6。
- **BERT模型**：BERT-large-uncased（Hugging Face预训练），最后四层解冻微调，其余层冻结。GPT-o4 mini用于标签生成。

## 5. 实验数量与充分性

- **实验组数**：主要包括：
  - 三个主要变体的性能对比（表1）。
  - 针对稀有食材（占据训练集<1.2%的类别，如枣、瓜、蔓越莓干、梨、无花果）的单独性能展示（表2）。
  - 消融实验（表3）：对Mask2Former+LIM-F和LIM-Q变体，调整分类损失权重（λ_cls从2到8）以及是否解冻BERT，共约6组。
  - 收敛曲线对比（图4）。
  - 定性结果示例（图5）。
- **充分性与公平性**：
  - 对比方法涵盖了近期主流（BEiTv2、IngredSAM、FoodSAM、Swin-TUNA、基线K-Net和Mask2Former），且在同一基准下报告。
  - 消融实验考察了损失权重和BERT微调的影响，揭示了分类损失权重增大有助于提升性能。
  - 训练细节（学习率、迭代次数等）对比基线保持基本一致。
  - **不足**：未在多个不同数据集（如Food-101、UECFood）上验证泛化性；未与更近期的多模态方法（如CLIP-based方法）做详细对比；未报告统计显著性检验。整体实验虽合理，但泛化性验证略有欠缺。

## 6. 论文的主要结论与发现

- LIM-Q在Mask2Former（Swin-L）上达到55.0 mIoU，超越所有现有方法，成为FoodSeg103新SOTA。
- LIM-F在相同骨干下获得54.4 mIoU，并且可推广至CNN架构（K-Net），提升幅度可观（+2.7%），证明了其通用性。
- 两个模块都显著改善了稀有食材和视觉相似食材的分割精度（表2），例如“date”从1.28提升至88.93（LIM-F）和77.31（LIM-Q）。
- LIM-Q收敛速度更快（图4），归因于更直接的文本语义注入。
- 仅需适度GPU内存增加（最多3.8 GB），无需特殊预训练，具有实用性和可扩展性。

## 7. 优点

- **即插即用设计**：无需修改骨干网络或解码器核心结构，可轻松集成到现有分割框架（CNN或Transformer）。
- **无需图文对齐数据**：利用LLM从图像直接推断食材标签，避免了Recipe1M+等大规模对齐数据集的依赖，降低了训练门槛。
- **轻量高效**：额外计算开销和内存占用小，训练收敛快。
- **多架构兼容**：LIM-F适用于多级输出骨干+任意解码器；LIM-Q专门优化Transformer解码器，两者互补。
- **在稀有类别上表现突出**：语义信息有效弥补视觉模糊性，显著提升长尾食材的分割质量。

## 8. 不足与局限

- **实验覆盖有限**：仅在单一数据集FoodSeg103上评估，未在Food-101、UECFood等常见食物分割数据集上验证，泛化性需要更多证据。
- **LLM标签生成依赖**：使用GPT-o4 mini在线生成，可能引入偏差或成本；未探索其他LLM（如Llama）或本地部署方案。
- **未与近期开放词汇方法（如CLIP-based）全面对比**：仅对比了OVFoodSeg（但未在表中列出其性能），未纳入CLIPSeg、GroupViT等。
- **消融实验不够深入**：未探讨不同层级LIM-F的独立贡献、不同LLM温度参数影响、不同BERT层数选择等。
- **公平性考虑不足**：未报告多次运行的标准差或统计显著性检验；超参数微调可能偏向于报告最佳结果。
- **应用限制**：当前方法仅适用于预先定义的食材类别列表，对于开放世界或未见过的食材可能无效；推理时仍需LLM先推断标签，可能增加延迟。

（完）
